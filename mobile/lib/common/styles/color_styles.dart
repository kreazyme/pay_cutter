import 'package:flutter/material.dart';

abstract class AppColors {
  static Color primaryColor = const Color(0xff59D9CC);
  static Color backgroundColor = primaryColor.withOpacity(0.1);
  static Color alertText = Colors.red;
  static Color borderColor = Colors.grey;
  static Color textColor = Colors.black87;
  static Color disableColor = Colors.grey;
}
