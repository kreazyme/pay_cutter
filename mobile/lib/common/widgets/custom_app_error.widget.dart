import 'package:flutter/material.dart';

class CustomAppErrorWidget extends StatelessWidget {
  const CustomAppErrorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
